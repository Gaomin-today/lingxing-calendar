# 验证记录

## v0.6 · 个人首页、河洛卦、旺衰初判与灵宠外观（2026-09-18）

版本：**0.6.0（build 7）**。

- `scripts/test.sh`：**211 项测试、25 个套件全部通过**。新增普通扶抑初判、河洛边界、外观配置和 Agent 任务文本测试；Unix socket 测试在允许本机 IPC 的环境执行。
- 河洛引擎对照私有参考脚本生成的 **173 组合成结果**，覆盖全部 64 卦映射；额外测试立春、跨年小寒、交节瞬间、当地零点及年限边界。参考脚本中发现的边界问题及原生修正已逐项记入 [河洛说明](heluo-v0.6.md)，不宣称未经修正的逐项复刻或独立天文精度证明。
- `scripts/test-cli-integration.py`：在隔离预览完成 **77 项端到端检查**，包括 `strength show`、`hexagrams show`、`context` 一致性、缺失资料、严格时间校验，以及手动／有效 Agent／本地初判的采用顺序。临时合成记录在结束后清理；既有界面演示资料保留。
- `scripts/test-apple-codec.sh`：**6 项提醒事项日期转换检查通过**，没有实例化 EventKit 或修改真实系统事项。
- Release 与隔离预览包均构建成功，版本为 0.6.0 / 7；两包 `codesign --verify --deep --strict` 校验通过。CLI、Skill、六篇内置知识及第三方许可随包保留。构建及测试日志保存于本机临时目录。
- 原生窗口通过 CUA 检查：个人首页、五类卦卡与值日详情、旺衰依据展开和有效 Agent 来源、外部任务文本与只读开关、复制反馈。卦象页选 **16:00** 后，交接界面与 `context` / `hexagrams show` 两条命令均保留同一 `--at '16:00'`。
- 六款原生向量灵宠已逐一检查联系表；实际切换成对月牙筊杯“双子”，采用主色黛蓝／辅色赭石并重启，灵宠、两种颜色和筛选均保留。第三项五行筛选明确提示上限；恢复默认正常。修复公共按钮样式未即时换色，实机确认切换朱砂后按钮同步更新。文字颜色的对比度和异常配置回退由外观测试覆盖。
- 本轮审查修复：交接任务遗漏卦象参考时刻、重复日程交接日期使用系列首日、日笺跳转依赖系统默认历法，以及换色后部分公共样式未刷新。海外时区首页明确标注对应当地日期，卦区间展示秒数并说明半开边界。
- 配套 Skill 校验与本地 Markdown 链接检查通过；随包新增河洛知识，保留资料来源和规则边界。完整私有 Skill、真实出生资料、个人日程和日记不进入提交或应用包。

本版的自动旺衰仅为可解释的普通扶抑初判，遇到未知时刻、候选盘、杂气月或结构争议会保留未定；没有自动确定喜用神。外部 Agent 入口生成可审阅的任务，复制本身不执行。新增 GitHub 工作流检查 PR 和 main，不能自动合并；远程执行状态以 Actions 实际结果为准。真实 Apple 同步、通知送达及远程模型验收仍保留在末尾清单。

## v0.5 · CLI、外部 Agent 与日笺回写（2026-09-17）

版本：**0.5.0（build 6）**。

- `scripts/test.sh`：**174 项测试、21 个套件全部通过**，日志 `build/test-results-v0.5.log`。首次在受限沙盒中有 5 项 Unix socket 绑定检查被系统拒绝；在允许本机 IPC 的沙盒外环境运行后全部通过。测试覆盖请求编解码、严格时间解析、socket 通信、写入凭据、崩溃后重放、版本冲突、损坏文件保护，以及既有历法与日程回归。
- `scripts/test-cli-integration.py`：通过正在运行的隔离预览包完成 **65 项检查**。验证创建／查询／更新／删除、档案版本、Agent 分析采用、参数拒绝、重复请求与合成数据清理；每条命令显式使用 `--preview`，写入前检查环境标识，未访问正式资料。
- `scripts/test-apple-codec.sh`：**6 项提醒事项日期转换检查通过**，未实例化 EventKit 或修改真实系统数据，日志 `build/apple-codec-v0.5.log`。
- Release 与隔离预览构建成功，版本均为 0.5.0 / 6；两份应用的 `codesign --verify --deep --strict` 校验通过。Release 构建日志为 `build/build-v0.5.log`。CLI、Skill、内置知识和第三方许可随包保留。
- Skill 的 `quick_validate` 通过，本地 Markdown 链接检查通过。按随包 Skill 实际查询 `status`、`capabilities`、知识检索／正文分页与指定参考时刻的日历，返回值和文档一致；随包 Skill 与审阅源码一致。
- 使用合成档案「演示 · 晨木」完成外部 Agent 工作流：读取实际四柱与知识，保存包含支持依据、相反证据和口径限制的初步偏弱分析，再读取明日上下文、保存日笺与本地演示日程。`context` 确认采用版本匹配的 `agent_insight`；四项写入均以相同请求 ID 重放，返回 `replayed: true`，没有重复创建。
- **原生窗口已通过 CUA 实际检查**：外部 Agent 接入面板、命盘分析卡与依据入口、日笺正文和来源标签，以及 CLI 打开的「演示 · 整理面试材料」编辑器。日程为 **2026-09-18 15:00–16:00，提前 15 分钟提醒**；接口返回 `preview_suppressed`，预览没有发送系统通知。
- 每日解读页面显示「Agent 分析 · 按身弱解读」，「进阶：临时比较不同旺衰前提」默认关闭。大运页面的原生截图中，十步大运横向排列、当运高亮和起运依据均正常可见。这也补验了 v0.4 当时因锁屏未完成的部分页面，不代表已穷尽历史版本的全部交互与布局组合。
- **跨窗口版本保护已实际验收**：打开日笺编辑器后，通过 CLI 修改同一记录标题，再点击窗口保存，界面显示版本冲突；CLI 再读确认较新的内容仍保留。验收后合成标题已恢复。重启新版预览后，以原请求 ID 重试仍返回 `replayed: true`，确认凭据跨进程保留。
- 本轮交互仅使用隔离预览中的合成数据，没有修改真实出生档案、日程或 Apple 事项。供界面检查的演示档案、两篇日笺和一条日程予以保留，ID、版本及请求记录位于忽略提交的 `build/v05-ui-fixture.json`；集成脚本自行创建的记录已清理。

本版仍需外部 Agent 进行旺衰解释，应用没有新增内置 Agent 框架或统一自动旺衰算法。CLI 只写本地事项；真实 Apple 账户授权／写入／跨设备同步、系统通知实际送达、远程模型服务及多显示器等未验收项目继续保留。详情见 [CLI 与 Skill 说明](cli-v0.5.md) 及本文末尾的未验收清单。

## v0.4 · 命盘、大运、个人解读与黄历（2026-09-17）

版本：**0.4.0（build 5）**。

- `scripts/test.sh`：最终 **141 项测试、15 个套件全部通过**，日志 `build/test-results-v0.4.log`。
- `scripts/test-apple-codec.sh`：**6 项提醒事项日期转换检查通过**；未实例化 EventKit 或修改真实系统数据，日志 `build/apple-codec-v0.4.log`。
- 两份新增 Python 参考生成器已运行：8 组真实八字引擎的命盘／起运样例、6 日期 × 13 时段的黄历样例与固定 `lunar_python 1.4.8` 对照。合成样例随源码保存，原始私有技能不发布。
- `scripts/build-app.sh`：最终 Release 构建成功，包中版本为 0.4.0 / 5，`codesign --verify --deep --strict` 通过；完整 MIT 许可随包保留。
- **本轮原生窗口交互与视觉验收未完成**：CUA 报告 Mac 已锁定，无法打开预览窗口；已请求解锁，没有尝试绕过锁屏。编译与测试不能替代新增页面的真实点击、滚动和布局验收。隔离预览中仅准备了合成档案，未修改真实个人档案或日程。
- 代码审查修正了跨档案临时旺衰假设继承、藏干次序过度解释、十神说明误用“流日”、当运参考瞬间标签、参考岁数口径、范围外流月跳转，以及历史夏令时与固定 UTC+8 黄历口径的区别。

- 新增命盘详情测试：对照真实八字技能生成的合成样例，检查藏干、十神、纳音、星运、自坐和旬空；覆盖全部六十甲子、未知时柱和晚子时换日影响。
- 新增大运测试：分钟法起运、顺逆排法、同一绝对出生瞬间的时区转换、夏令时间隔、闰日交运、十年区间首尾归属、立春流年、十二节月及 2100 边界补齐。
- 新增个人解读测试已在独立 scratch 中编译并运行：**8 项通过**。覆盖流年／月／日十神对象、十二月支本气、同类和生扶归属、不同阴阳的同五行根气、未知时柱，以及强弱假设只改变解释而不改变事实。
- 日时黄历使用实际 `wannianli.engine` 生成的 6 个合成日期和每个日期全部 13 时段作对照；新增并发缓存、早晚子时、九宫完整性及范围错误测试。节气保留 HKO 的 72 个日期回归，并检查 1901–2099 年每年 24 个节气、节与中气边界。
- 旧出生档案新增可选排运性别与旺衰假设；缺项保持未填，未知时刻不补柱。命盘和个人解读不自动发送至远程对话。文档分别说明民用日历、精确四柱、日时黄历的时间口径。

已实现范围见 [v0.4 八字与日运](bazi-v0.4.md)、[v0.4 黄历与来源](almanac-v0.4.md)。本版没有自动旺衰判定、真太阳时或完整旺衰喜用推断；原有 Apple 账户真实写入／同步、通知实际送达和远程模型服务仍需相应权限与配置下的验收。

## v0.3 · 四柱与出生档案（2026-09-17）

- `scripts/test.sh`：**105 项测试、10 个套件通过**，日志 `build/test-results-v0.3.log`；覆盖交节瞬间、晚子时、海外时区、未知时刻候选、出生档案日期／DST 校验、损坏保护，以及十神和逐柱合冲。原有日程、通知、Apple 转入回归保留。
- `scripts/test-apple-codec.sh`：6 项真实桥接日期转换断言通过，未实例化 EventKit 或写入系统账户。
- `ReferenceSkillsFixtureTests` 读取 8 组持久化合成样例：实际 `wannianli.engine` 6 组、实际 `bazi_chart.engine` 标准时模式 1 组、`lunar_python` sect 2 补充 1 组；与原生 Swift 结果全部一致。共享天文算法家族的一致性属于兼容核验，不能替代独立天文精度验证。
- 六个独立 HKO 分钟级交节参考点全部通过，2025／2026 立春、2026 夏至／白露／秋分／冬至四舍五入到分钟一致；不宣称全支持范围的秒级准确。
- 原生隔离预览：四柱页面、命盘、天干十神、流日关系／来源／行动建议均已查看，滚动区域无截断；实际新增合成档案，拒绝 2 月 30 日，保存并重载。
- 实际 UI 将 1988-02-15 23:30 的日界从零点切到 23 点：日柱由庚子变辛丑，时柱均戊子，十神与流日关系正确重算。切换为时间不详后显示两个无时柱候选并停止单一日运解读。删除经过确认后回到空档案状态，合成档案已清理。
- 代码审查修复：新日期选择同步 visibleMonth，避免 EventKit 跨多年加载；参考时刻严格解析夏令时空缺，不静默改成另一时刻；命盘十神直接调用十神接口；民用历法与八字口径分开说明。
- 从用户 ZIP 迁入两个完整技能（473 文件）供本地参考，保留逐文件校验清单；不进入应用包或 Git。出生档案与日程独立保存，未向云端聊天发送档案。
- Release v0.3.0（build 4）构建成功，`codesign --verify --deep --strict` 通过，应用包包含完整 MIT 第三方许可。

本批不包括真太阳时、大运、完整旺衰喜用或综合吉凶评分。原有 Apple 账户真实写入／同步、通知实际送达和远程模型服务的实机限制仍然适用。

## v0.2.1 · 修复保存位置误解（2026-09-17）

- 实际界面确认反馈原因：用户新建事项标为本地日程，Apple 日历显示未连接；不是已写入 EventKit 后的同步丢失。
- 增加明确的本地不写入 Apple 提示、连接入口、可记住的日程／待办默认位置，聊天草稿显示并使用同一默认位置。来源失效不静默降级。
- 增加已有本地项目确认转入 Apple。写入后检查回执再清理本地；清理失败尝试补偿；补偿失败保留回执，仅重试清理。旧本地编辑器不重复创建，系统副本丢失或改变时保留本地。内存回执不承诺进程崩溃时原子性。
- `scripts/test.sh`：64 项测试、6 个套件通过；新增 6 项转入行为测试，覆盖失败重试、补偿、重复提交、独立记录和外部副本丢失。`scripts/test-apple-codec.sh`：6 项日期转换断言通过。
- 原生隔离预览检查：已有本地事项编辑器显示保存位置、本地提示和转入入口；嵌套连接页可以打开和返回。测试未授予权限，也未写入真实 Apple 账户。
- Release v0.2.1 构建并启动成功，用户原有本地事项仍在；正式连接页面仍显示 Apple 日历未连接。真实系统写入和迁移需用户授权后验收。

## v0.2 · 第一批（2026-09-17）

- `scripts/test.sh`：58 项测试、5 个测试套件通过，日志在 `build/test-results-v0.2.log`。包括原有历法／自然语言／持久化，以及新增的周范围、跨日裁切、重叠布局、空档、冲突、通知版本与容量规划。
- `scripts/test-apple-codec.sh`：6 项 Apple 提醒事项日期转换断言通过，覆盖无时刻截止日期、跨时区时刻及往返保存。编译实际桥接代码，运行时不实例化 EventKit、不访问用户数据。
- `scripts/build-app.sh`：Release 构建成功，生成 v0.2.0 原生应用；`scripts/build-preview.sh` 生成独立 bundle ID、独立 JSON 数据文件的预览包。本轮 UI 写入均使用明确标为「预览」的合成数据，预览不安排本地通知。
- 正式包通过 `codesign --verify --deep --strict` 校验，重启后界面显示 v0.2、月／周／日切换和来源连接入口。真实本地日程保持原样，未注入合成记录。
- 界面验收：月／周／日切换、日视图时辰、重叠日程并排、跨午夜显示、全天区与空档入口、来源标识、连接页面均已检查。
- 持久化验收：编辑合成日程标题后在时间轴和 JSON 中一致；待办完成后移入已完成，恢复后重新进入不设期限分组。
- 冲突验收：编辑器列出重叠项目，保存时出现明确确认；聊天输入“提醒我今天下午三点读书，不用提醒”，解析为 15:00–16:00，点击加入后进入编辑器并正确指出 15:00–15:30 的既有安排。测试草稿已取消。
- 代码审查修复：空档新建时长超过空档、本地重复实例拖动误用系列首日、聊天绕过冲突确认、权限撤销后的旧异步结果回填、只改标题误清空系统日期型提醒、系统日期型待办跨时区偏移、通知完成动作未等待持久化成功。

尚未实机验收：真实 Apple 账户的授权／读写／跨设备同步／权限撤销；通知横幅、稍后提醒及通知按钮实际送达；日程拖动在不同缩放和显示器上的表现。未替用户授予日历或提醒事项权限，也未修改真实系统事项。测试通知需用户开启通知后自行触发。完整 Agent、年视图与 Widget 不属于本批范围。

## v0.1 · 初版

日期：2026-09-17。环境：Apple Silicon、macOS 26.6.2、Swift 6.3.3；仅安装 Command Line Tools。

- `scripts/test.sh`：40 项测试通过，3 个测试套件。10 项历法，19 项解析与离线建议，11 项日程持久化／重复／提醒。
- `scripts/build-app.sh`：Release 构建成功，生成原生 `.app`。
- `codesign --verify --deep --strict`：本机 ad-hoc 签名校验成功。
- 实际启动：通过桌面应用入口成功启动并显示完整原生界面。受限 shell 中的 `open` 失败不代表 `.app` 缺少可执行文件；桌面入口与后续重新启动已验证。
- 视觉检查：米白／青绿月视图、42格农历标注、日详情、聊天与原生日程编辑器可见，未发现主视图遮挡或截断。详情可滚动。
- UI 日期检查：2026-09-25 展示八月十五、中秋节、地域说明及央视来源链接；新建编辑器默认选中日期、开始结束时间及提醒值正确。
- 快捷键与菜单：已改为 AppKit 统一管理，⌘N 新建日程、⌘J 独立聊天窗口、⌘, 偏好设置均通过实际 UI 验证。
- 对话草稿：实际输入“提醒我明天下午三点面试，不用提醒”，正确显示 2026-09-18 15:00、60 分钟、不重复、不提醒；随后输入缺少时间的“提醒我出行”，正确追问并移除旧草稿。本轮未将测试草稿写入日历。

代码审查后修正：通知异步重建的过期请求残留、无权限时清理旧通知、重复通知回到实际发生日期、损坏数据保留现有通知、未保存AI设置立即生效、跨服务主机复用密钥、澄清指令保留旧草稿。

## 尚未验收

- 没有配置远程服务凭据，真实云端对话未测试。
- 本机设置页显示“系统通知已关闭”；未替用户更改系统权限。需开启后检查实际横幅，单元测试不能替代送达验收。
- 未在 Intel Mac 或 macOS 14/15 设备上实机验收。
- 桌面拖动使用 AppKit 原生窗口拖动句柄，未进行多显示器／全屏 Spaces 组合验收。
- 开发包尚未使用 Developer ID 签名或公证，不用于公开分发。
